[pytest]
markers =
    network: testes que fazem chamadas reais a APIs externas (precisa de internet)
    slow: testes mais lentos (carregam modelos GNN, rodam relaxação, ou chamam o LLM)
testpaths = tests
